import server.Usuario

model {
	Usuario usuario
}

json {
	id usuario.id
	papeis usuario.papeis.id
	senha usuario.senha
	ativo usuario.ativo
	email usuario.email
	link {
		href "${g.link(resource: 'usuario', absolute: true)}/${usuario.id}"
	}
}
